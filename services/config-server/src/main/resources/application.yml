spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
            searchLocations: classpath:/configurations

server:
  port: 8888